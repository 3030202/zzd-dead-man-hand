## 2026-01-26 - CLI Output Formatting
**Learning:** Raw JSON output in CLI tools is difficult for users to read. Pretty-printing by default significantly improves the user experience without changing functionality.
**Action:** Always implement JSON pretty-printing for CLI commands that output structured data, with a raw fallback for non-JSON responses.
