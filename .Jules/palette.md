## 2024-05-23 - Pretty Printing JSON Output
**Learning:** CLI users often struggle to read raw JSON output.
**Action:** When a CLI command returns JSON data, always attempt to pretty-print it. This makes the output much more readable and accessible for humans. Fallback to raw output if it's not valid JSON.
