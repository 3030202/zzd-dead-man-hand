## 2026-01-20 - CLI Output Readability
**Learning:** Users of CLI tools expect structured data (JSON) to be readable by default, not minified.
**Action:** Default to pretty-printed JSON output in CLI commands, with a fallback to raw output if parsing fails.
